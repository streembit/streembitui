'use strict';

var streembit = streembit || {};
streembit.services = streembit.services || {};



define(['knockout', 'jquery'], function (ko, $) {
    var logger = {
        

    };

    return logger;
});

